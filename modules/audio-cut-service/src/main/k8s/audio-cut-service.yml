apiVersion: v1
kind: Service
metadata:
  name: audio-cut-service
  namespace: ${k8s.namespace}
spec:
  selector:
    app: audio-cut-service
  ports:
    - port: 12000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audio-cut-service
  namespace: ${k8s.namespace}
  labels:
    app: audio-cut-service
spec:
  replicas: ${audio-cut-service.replicas}
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: audio-cut-service
  template:
    metadata:
      labels:
        app: audio-cut-service
    spec:
      containers:
        - name: audio-cut-service
          image: "${docker.image.name}:latest"
          imagePullPolicy: Always
          ports:
            - containerPort: 12000
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "${spring.profiles.active}"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 12000
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 3
            successThreshold: 2
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 12000
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 3